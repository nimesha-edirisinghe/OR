// import { rest } from 'msw';
// import { jobScheduleResponse } from 'mocks/fixtures/jobScheduleMockData';

// export const handlers = [
//   rest.get('https://or-ent-dev.algonomy.com/api/v1/schedule/getSchedules', (req, res, ctx) => {
//     return res(ctx.status(200), ctx.json(jobScheduleResponse));
//   })
// ];

export const handlers = [];
