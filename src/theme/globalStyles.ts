import { ocean_blue_700 } from './colors';

export const globalStyles = {
  // initialColorMode: "dark",
  // useSystemColorMode: false,
  global: {
    body: {
      background: ocean_blue_700
    }
  }
};
